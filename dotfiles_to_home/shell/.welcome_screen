# Added startup program CLM 

# Adjust monitor resolution
~/bin/monitor.sh

# Python nuggets
~/bin/pynuggets_univ.py -r

# ssh agent
# eval $(ssh-agent)
# call .bash_aliases
[[ -f ~/.bash_aliases ]] && . ~/.bash_aliases

# Keep  notify-lib messages
notify-log.sh ~/.log/notify.log &

